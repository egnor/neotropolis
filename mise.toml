[tools]
python = "3.12"
uv = "latest"

[settings]
experimental = true
python.uv_venv_auto = true

[hooks]
postinstall = "uv sync"

[tasks]
check = ["uv sync", "black --check .", "ruff check", "mypy", "pytest"]
